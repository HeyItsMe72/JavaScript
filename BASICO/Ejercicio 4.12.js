/*Función que determina si un # es primo.*/

let numero = (numero) => {
    if (typeof numero !== "number") 
    return console.error ("No es un número"); 
    if(numero === undefined) 
    return console.warn("No introduciste número"); 
    else {

    }
}